import { Inbox } from "lucide-react";

const submissionRoutes = [
  {
    path: "/submissions/all-submissions",
    sideBarProps: {
      displayText: "All Submissions",
      icon: <Inbox />,
    },
  },
];

export default submissionRoutes;
